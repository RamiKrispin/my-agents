Drop real posts here. One file per post. Each file is just the post text — no
frontmatter, no commentary. See `../README.md` for the full convention.

This folder defines the **list / top-N** template — posts shaped like
"5 Python libraries for time series forecasting" or "10 Dockerfile mistakes I
see in AI projects".
